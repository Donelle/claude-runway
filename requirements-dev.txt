# Dev-only tooling for CONTRIBUTING to this repo -- not needed to USE the
# toolkit (that's requirements.txt, which stays scoped to what an end user
# installing ClaudeRunway into their own project actually needs). Install
# after requirements.txt, in the same venv:
#   uv pip install -r requirements-dev.txt --index-url https://pypi.org/simple

# Type checking -- config lives in mypy.ini. Run with:
#   .venv/bin/mypy libs tools hooks
mypy==2.3.1
pytest

# Issue #206: tests/test_pyproject.py builds a REAL wheel (via `python -m
# build --wheel --no-isolation`) and inspects its actual contents, rather
# than only parsing pyproject.toml's static tables -- setup.py's data_files
# glob logic can't be pinned any other way, since it's real Python code
# that runs at build time, not declarative TOML. `--no-isolation` reuses
# this venv's own already-installed `setuptools`/`wheel` instead of `build`
# creating a fresh isolated env per invocation (which would otherwise need
# network access on every test run just to re-fetch setuptools).
setuptools>=68
wheel
build
